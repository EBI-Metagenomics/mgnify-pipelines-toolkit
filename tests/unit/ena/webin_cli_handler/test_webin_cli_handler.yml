- name: assembly_validation_test_success
  tags:
    - assembly_validation_success
  exit_code: 1
  command:
    webin_cli_handler -c genome -m tests/fixtures/webin_cli_handler/assembly.manifest --mode validate --test --workdir ??
  stdout:
    contains:
      - "Command completed successfully"
      - "Submission/validation done for tests/fixtures/webin_cli_handler/assembly.manifest"

# todo assembly submit to test
# todo assembly submit same to test
# todo genome validate test
# todo genome submit test
